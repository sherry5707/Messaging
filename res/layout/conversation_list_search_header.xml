<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@color/conversation_list_diliver_favorite_color"
        android:paddingLeft="16dp"
        android:paddingRight="16dp"
        android:paddingTop="8dp"
        android:paddingBottom="8dp">

        <TextView
            android:id="@+id/search_view"
            android:layout_width="match_parent"
            android:layout_height="@dimen/search_edit_text_height"
            android:layout_alignParentLeft="true"
            android:layout_centerVertical="true"
            android:background="@drawable/bg_conversation_list_search_header"
            android:descendantFocusability="blocksDescendants"
            android:gravity="center_vertical"
            android:paddingLeft="6dp"
            android:drawableLeft="@drawable/ic_search_msg"
            android:drawablePadding="13dp"
            android:text="@string/menu_search"
            android:textColor="@color/search_view_hint_color"
            android:textSize="13dp" />

    </LinearLayout>

    <View
        android:layout_width="match_parent"
        android:layout_height="@dimen/toolbar_bottom_diliver_height"
        android:background="@color/toolbar_diliver"
        android:layout_gravity="bottom"/>
</LinearLayout>
